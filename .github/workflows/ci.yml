name: Continuous Integration (CI)

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  backend-check:
    name: Backend Lint & Typecheck
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Verificar compilación TypeScript
        run: npm run build

  frontend-check:
    name: Frontend Lint & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Ejecutar Linter
        run: npm run lint

      - name: Verificar Build de Producción
        run: npm run build

  e2e-smoke:
    name: E2E Smoke Tests (Playwright Chromium)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    env:
      CI: true
      VITE_CLERK_PUBLISHABLE_KEY: ${{ secrets.VITE_CLERK_PUBLISHABLE_KEY || 'pk_test_Zmx1ZW50LW1vb3NlLTkwNjAuY2xlcmsuYWNjb3VudHMuZGV2JA' }}
      CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY || 'sk_test_A7X9QzQD66LtWJx1ZiupnnxFseRsYcqUTx34Y2OQA1' }}
      E2E_CLERK_TEST_EMAIL: ${{ secrets.E2E_CLERK_TEST_EMAIL || 'admin.panaderia@hotmail.com' }}
      E2E_CLERK_TEST_PASSWORD: ${{ secrets.E2E_CLERK_TEST_PASSWORD || 'MargenXDev2026.' }}
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias del frontend
        run: npm ci

      - name: Instalar navegador Chromium para Playwright
        run: npx playwright install --with-deps chromium

      - name: Ejecutar Smoke Test E2E
        run: npx playwright test tests/e2e/smoke.spec.ts --project=chromium

      - name: Subir reporte Playwright en caso de fallo
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 7

  notify-ci-failure:
    name: Notificar Fallo en Discord
    runs-on: ubuntu-latest
    needs: [backend-check, frontend-check, e2e-smoke]
    if: failure()
    steps:
      - name: Enviar alerta a Discord
        continue-on-error: true
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RUN_URL: "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          BRANCH_OR_REF: "${{ github.head_ref || github.ref_name }}"
          AUTHOR: "${{ github.actor }}"
          COMMIT_MSG: "${{ github.event.head_commit.message || github.event.pull_request.title || 'Commit en PR' }}"
        run: |
          if [ -z "$DISCORD_WEBHOOK" ]; then
            echo "⚠️ DISCORD_WEBHOOK_URL no configurado. Omitiendo notificación."
            exit 0
          fi

          PAYLOAD=$(jq -n \
            --arg content "🚨 **Fallo en Pipeline de CI — MargenX**" \
            --arg title "Falló la verificación de CI (Lint / Build / E2E Tests)" \
            --arg run_url "$RUN_URL" \
            --arg branch "$BRANCH_OR_REF" \
            --arg author "$AUTHOR" \
            --arg commit "$COMMIT_MSG" \
            '{
              content: $content,
              embeds: [
                {
                  title: $title,
                  url: $run_url,
                  color: 15158332,
                  fields: [
                    { name: "Rama / Ref", value: $branch, inline: true },
                    { name: "Disparado por", value: $author, inline: true },
                    { name: "Mensaje / PR", value: $commit, inline: false },
                    { name: "Detalles del Error", value: ("[Ver Logs en GitHub Actions](" + $run_url + ")"), inline: false }
                  ],
                  timestamp: (now | todate)
                }
              ]
            }')

          curl -s -S -H "Content-Type: application/json" -X POST -d "$PAYLOAD" "$DISCORD_WEBHOOK" || echo "Error enviando notificación a Discord"